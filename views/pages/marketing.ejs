<%- include( '../' +'components/header'); -%>

<main class="flex items-center rounded-lg gap-4 flex-col">
  <h1 class="text-[#111827] text-[2.5rem] text-center tracking-[-2px] font-black antialiased capitalize"><% if(locals.page) {%> <%= locals.page %> <% } else { %> Marketing page <%} %></h1>
  <section class="w-full md:mx-auto md:max-w-screen-md self-start"><% if(locals.content) {%> <%= locals.content %> <% } else { %> Marketing content <%} %></section>
</main>